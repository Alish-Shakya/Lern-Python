# program to count the occurance of a element in a tupple 

# Create a tuple
numbers = (10, 20, 30, 20, 40, 20, 50)

# Element to count
element = 20

# Count occurrences
count = numbers.count(element)

# Display result
print(f"The element {element} appears {count} times in the tuple.")
